<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Reservierung_l_uft_aus_in__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
  reserviert_bis__c - NOW() &gt; 0 ,
  TEXT( FLOOR( reserviert_bis__c - NOW() ) ) &amp; &quot; Tagen &quot;
  &amp; TEXT( FLOOR( MOD( (reserviert_bis__c - NOW() ) * 24, 24 ) ) ) &amp; &quot; Stunden &quot;
  &amp; TEXT( ROUND( MOD( (reserviert_bis__c - NOW() ) * 24 * 60, 60 ), 0 ) ) &amp; &quot; Minuten&quot;,
  &quot;&quot;
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Reservierung läuft aus in</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
